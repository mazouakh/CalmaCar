<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_home"
            android:title="Recherche"
            android:icon="@drawable/ic_map"/>
        <item
            android:id="@+id/nav_tripsHistory"
            android:title="Historique Trajets"
            android:icon="@drawable/ic_add_trip"/>
    </group>
    <item
        android:title="Profile">
        <menu>
            <group android:checkableBehavior="single">
            <item
                android:id="@+id/nav_profile"
                android:title="Profile"
                android:icon="@drawable/ic_profile"/>
            <item
                android:id="@+id/nav_driver"
                android:title="Switch Conducteur"
                android:icon="@drawable/ic_driver"/>
            <item
                android:id="@+id/nav_logout"
                android:title="Deconexion"
                android:icon="@drawable/ic_logout"/>
            </group>
        </menu>
    </item>
</menu>